extends layout

block content
  h1= title
  p Welcome to #[b Local Library], a little project of the MDN Documentation created with Expres & Mongoose.

  h2 Dynamic Content:

  if err
    h3 Sorry but we have troubles getting the content, please try later
  else
    h3 The library has the following record counts:

    ul
      li #[strong Books:] !{data.book_count}
      li #[strong Copies:] !{data.book_instance_count}
      li #[strong Available Copies:] !{data.book_instance_avaliable_count}
      li #[strong Authors:] !{data.author_count}
      li #[strong Genres:] !{data.genre_count}
